# Base 42 vcpkg registry

The dock(vcpkg registry) of Base 42, where you can browse through the `ports` folder for a list of available libraries

## Getting started

Firstly, set and install vcpkg. Find a good detailed guide @ https://github.com/microsoft/vcpkg 
